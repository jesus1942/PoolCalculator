import React, { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { useAuth } from '@/context/AuthContext';

export const Landing: React.FC = () => {
  const { isAuthenticated } = useAuth();
  const [scrollY, setScrollY] = useState(0);
  const [isVisible, setIsVisible] = useState({
    hero: false,
    about: false,
    features: false,
    benefits: false,
    cta: false,
  });

  useEffect(() => {
    const handleScroll = () => {
      setScrollY(window.scrollY);

      const sections = ['hero', 'about', 'features', 'benefits', 'cta'];
      sections.forEach((section) => {
        const element = document.getElementById(section);
        if (element) {
          const rect = element.getBoundingClientRect();
          const isInView = rect.top < window.innerHeight * 0.75;
          if (isInView && !isVisible[section as keyof typeof isVisible]) {
            setIsVisible((prev) => ({ ...prev, [section]: true }));
          }
        }
      });
    };

    window.addEventListener('scroll', handleScroll);
    handleScroll();
    return () => window.removeEventListener('scroll', handleScroll);
  }, [isVisible]);

  return (
    <div className="min-h-screen bg-black">
      {/* Fixed gradient backgrounds */}
      <div className="fixed inset-0 bg-gradient-to-br from-zinc-900 via-black to-black pointer-events-none -z-10"></div>
      <div className="fixed inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-cyan-950/20 via-transparent to-transparent pointer-events-none -z-10"></div>

      {/* Navigation */}
      <nav className="fixed top-0 left-0 right-0 z-50 bg-zinc-950/80 backdrop-blur-xl border-b border-zinc-800/50 transition-all duration-300">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between items-center h-16">
            <div className="flex items-center space-x-3">
              <div className="w-10 h-10 rounded-xl bg-gradient-to-br from-cyan-500 to-blue-600 flex items-center justify-center shadow-lg">
                <div className="w-6 h-6 rounded-full border-2 border-white"></div>
              </div>
              <span className="text-xl font-light text-white tracking-wide">
                Pool <span className="font-semibold">Calculator</span>
              </span>
            </div>

            <div className="flex items-center space-x-4">
              {isAuthenticated ? (
                <Link
                  to="/dashboard"
                  className="px-6 py-2 rounded-xl bg-cyan-500/10 text-cyan-400 border border-cyan-500/30 hover:bg-cyan-500/20 transition-all duration-200 font-light"
                >
                  Ir al Dashboard
                </Link>
              ) : (
                <>
                  <Link
                    to="/login"
                    className="px-6 py-2 rounded-xl text-zinc-400 hover:text-white hover:bg-zinc-800/50 transition-all duration-200 font-light"
                  >
                    Iniciar Sesión
                  </Link>
                  <Link
                    to="/register"
                    className="px-6 py-2 rounded-xl bg-cyan-500/10 text-cyan-400 border border-cyan-500/30 hover:bg-cyan-500/20 transition-all duration-200 font-light"
                  >
                    Registrarse
                  </Link>
                </>
              )}
            </div>
          </div>
        </div>
      </nav>

      {/* Hero Section */}
      <section
        id="hero"
        className={`relative min-h-screen flex items-center justify-center px-4 transition-all duration-1000 ${
          isVisible.hero ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'
        }`}
      >
        <div className="max-w-5xl mx-auto text-center">
          <h1 className="text-5xl md:text-7xl font-light text-white mb-6 tracking-tight">
            Gestión Profesional de{' '}
            <span className="font-semibold bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">
              Proyectos de Piscinas
            </span>
          </h1>
          <p className="text-xl md:text-2xl text-zinc-400 mb-12 font-light max-w-3xl mx-auto leading-relaxed">
            Plataforma completa para calcular, planificar y gestionar la construcción de piscinas con precisión profesional.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            {isAuthenticated ? (
              <Link
                to="/dashboard"
                className="px-8 py-4 rounded-xl bg-gradient-to-r from-cyan-500 to-blue-600 text-white font-light hover:shadow-2xl hover:shadow-cyan-500/50 transition-all duration-300 hover:scale-105"
              >
                Acceder al Dashboard
              </Link>
            ) : (
              <>
                <Link
                  to="/register"
                  className="px-8 py-4 rounded-xl bg-gradient-to-r from-cyan-500 to-blue-600 text-white font-light hover:shadow-2xl hover:shadow-cyan-500/50 transition-all duration-300 hover:scale-105"
                >
                  Comenzar Ahora
                </Link>
                <Link
                  to="/login"
                  className="px-8 py-4 rounded-xl bg-zinc-900/50 text-zinc-300 border border-zinc-800/50 font-light hover:bg-zinc-800/50 transition-all duration-300"
                >
                  Ver Demo
                </Link>
              </>
            )}
          </div>
        </div>

        {/* Floating elements animation */}
        <div
          className="absolute inset-0 overflow-hidden pointer-events-none opacity-20"
          style={{ transform: `translateY(${scrollY * 0.5}px)` }}
        >
          <div className="absolute top-20 left-10 w-72 h-72 bg-cyan-500/10 rounded-full blur-3xl"></div>
          <div className="absolute bottom-20 right-10 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl"></div>
        </div>
      </section>

      {/* About Section */}
      <section
        id="about"
        className={`relative py-32 px-4 transition-all duration-1000 delay-200 ${
          isVisible.about ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'
        }`}
      >
        <div className="max-w-6xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-4xl md:text-5xl font-light text-white mb-6">
              La Herramienta Definitiva para{' '}
              <span className="font-semibold text-cyan-400">Profesionales</span>
            </h2>
            <p className="text-xl text-zinc-400 font-light max-w-3xl mx-auto">
              Pool Calculator revoluciona la forma en que los constructores de piscinas gestionan sus proyectos,
              ofreciendo cálculos precisos, gestión de materiales y seguimiento en tiempo real.
            </p>
          </div>

          <div className="grid md:grid-cols-2 gap-8">
            <div className="bg-zinc-950/50 backdrop-blur-xl border border-zinc-800/50 rounded-2xl p-8 hover:border-cyan-500/30 transition-all duration-300">
              <h3 className="text-2xl font-light text-white mb-4">Cálculos Automáticos</h3>
              <p className="text-zinc-400 font-light leading-relaxed">
                Sistema inteligente que calcula automáticamente excavación, materiales de construcción, revestimientos,
                plomería y equipamiento necesario basándose en las dimensiones de la piscina.
              </p>
            </div>

            <div className="bg-zinc-950/50 backdrop-blur-xl border border-zinc-800/50 rounded-2xl p-8 hover:border-cyan-500/30 transition-all duration-300">
              <h3 className="text-2xl font-light text-white mb-4">Gestión de Proyectos</h3>
              <p className="text-zinc-400 font-light leading-relaxed">
                Organiza todos tus proyectos en un solo lugar. Crea tareas personalizadas, asigna roles,
                calcula costos de mano de obra y mantén el control total del presupuesto.
              </p>
            </div>

            <div className="bg-zinc-950/50 backdrop-blur-xl border border-zinc-800/50 rounded-2xl p-8 hover:border-cyan-500/30 transition-all duration-300">
              <h3 className="text-2xl font-light text-white mb-4">Recomendaciones de Equipamiento</h3>
              <p className="text-zinc-400 font-light leading-relaxed">
                Obtén recomendaciones precisas de bombas, filtros, calefactores y accesorios basadas en el volumen
                de la piscina y las especificaciones técnicas del proyecto.
              </p>
            </div>

            <div className="bg-zinc-950/50 backdrop-blur-xl border border-zinc-800/50 rounded-2xl p-8 hover:border-cyan-500/30 transition-all duration-300">
              <h3 className="text-2xl font-light text-white mb-4">Exportación Profesional</h3>
              <p className="text-zinc-400 font-light leading-relaxed">
                Genera presupuestos y especificaciones técnicas en formato Excel con un click.
                Documentos profesionales listos para presentar a tus clientes.
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* Features Section */}
      <section
        id="features"
        className={`relative py-32 px-4 transition-all duration-1000 delay-300 ${
          isVisible.features ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'
        }`}
      >
        <div className="max-w-6xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-4xl md:text-5xl font-light text-white mb-6">
              Características <span className="font-semibold text-cyan-400">Principales</span>
            </h2>
            <p className="text-xl text-zinc-400 font-light max-w-3xl mx-auto">
              Todo lo que necesitas para gestionar tus proyectos de construcción de piscinas de manera profesional.
            </p>
          </div>

          <div className="grid md:grid-cols-3 gap-6">
            {[
              {
                title: 'Modelos Preconfigurados',
                description: 'Amplia biblioteca de diseños de piscinas con dimensiones y especificaciones predefinidas.',
              },
              {
                title: 'Cálculo de Excavación',
                description: 'Dimensiones exactas de excavación incluyendo espacios de colchón lateral y de piso.',
              },
              {
                title: 'Materiales de Construcción',
                description: 'Cantidades precisas de cemento, arena, malla electrosoldada y geomembrana.',
              },
              {
                title: 'Revestimientos',
                description: 'Cálculo de losetas y venecitas con configuración personalizable de áreas.',
              },
              {
                title: 'Sistema de Plomería',
                description: 'Lista detallada de codos, tees, válvulas y cañería según el diseño.',
              },
              {
                title: 'Dashboard Inteligente',
                description: 'Visualización de métricas, proyectos activos y análisis de rentabilidad.',
              },
              {
                title: 'Asistente de Ubicación',
                description: 'Herramienta visual para verificar que la piscina encaje en el terreno disponible.',
              },
              {
                title: 'Gestión de Roles',
                description: 'Define tarifas horarias y diarias para diferentes profesionales del equipo.',
              },
              {
                title: 'Portal del Cliente',
                description: 'Los clientes pueden ver el progreso de su proyecto en tiempo real mediante un link compartido.',
              },
            ].map((feature, index) => (
              <div
                key={index}
                className="bg-zinc-950/50 backdrop-blur-xl border border-zinc-800/50 rounded-2xl p-6 hover:border-cyan-500/30 hover:bg-zinc-900/50 transition-all duration-300 group"
                style={{ transitionDelay: `${index * 50}ms` }}
              >
                <h3 className="text-xl font-light text-white mb-3 group-hover:text-cyan-400 transition-colors">
                  {feature.title}
                </h3>
                <p className="text-zinc-400 font-light leading-relaxed text-sm">
                  {feature.description}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Benefits Section */}
      <section
        id="benefits"
        className={`relative py-32 px-4 transition-all duration-1000 delay-400 ${
          isVisible.benefits ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'
        }`}
      >
        <div className="max-w-6xl mx-auto">
          <div className="bg-gradient-to-br from-cyan-950/20 to-blue-950/20 backdrop-blur-xl border border-cyan-500/20 rounded-3xl p-12">
            <h2 className="text-4xl md:text-5xl font-light text-white mb-8 text-center">
              Por Qué Elegir <span className="font-semibold text-cyan-400">Pool Calculator</span>
            </h2>

            <div className="grid md:grid-cols-2 gap-8 mt-12">
              <div className="space-y-6">
                <div className="flex items-start space-x-4">
                  <div className="w-8 h-8 rounded-lg bg-cyan-500/20 border border-cyan-500/30 flex-shrink-0"></div>
                  <div>
                    <h4 className="text-xl font-light text-white mb-2">Ahorra Tiempo</h4>
                    <p className="text-zinc-400 font-light">
                      Lo que antes tomaba horas de cálculos manuales, ahora se hace en segundos.
                    </p>
                  </div>
                </div>

                <div className="flex items-start space-x-4">
                  <div className="w-8 h-8 rounded-lg bg-cyan-500/20 border border-cyan-500/30 flex-shrink-0"></div>
                  <div>
                    <h4 className="text-xl font-light text-white mb-2">Precisión Garantizada</h4>
                    <p className="text-zinc-400 font-light">
                      Algoritmos verificados que eliminan errores humanos en los cálculos.
                    </p>
                  </div>
                </div>

                <div className="flex items-start space-x-4">
                  <div className="w-8 h-8 rounded-lg bg-cyan-500/20 border border-cyan-500/30 flex-shrink-0"></div>
                  <div>
                    <h4 className="text-xl font-light text-white mb-2">Profesionalismo</h4>
                    <p className="text-zinc-400 font-light">
                      Presenta presupuestos detallados y profesionales que inspiran confianza.
                    </p>
                  </div>
                </div>
              </div>

              <div className="space-y-6">
                <div className="flex items-start space-x-4">
                  <div className="w-8 h-8 rounded-lg bg-cyan-500/20 border border-cyan-500/30 flex-shrink-0"></div>
                  <div>
                    <h4 className="text-xl font-light text-white mb-2">Control Total</h4>
                    <p className="text-zinc-400 font-light">
                      Mantén todos tus proyectos organizados y accesibles desde cualquier dispositivo.
                    </p>
                  </div>
                </div>

                <div className="flex items-start space-x-4">
                  <div className="w-8 h-8 rounded-lg bg-cyan-500/20 border border-cyan-500/30 flex-shrink-0"></div>
                  <div>
                    <h4 className="text-xl font-light text-white mb-2">Escalable</h4>
                    <p className="text-zinc-400 font-light">
                      Desde un proyecto pequeño hasta gestionar decenas de construcciones simultáneas.
                    </p>
                  </div>
                </div>

                <div className="flex items-start space-x-4">
                  <div className="w-8 h-8 rounded-lg bg-cyan-500/20 border border-cyan-500/30 flex-shrink-0"></div>
                  <div>
                    <h4 className="text-xl font-light text-white mb-2">Soporte Continuo</h4>
                    <p className="text-zinc-400 font-light">
                      Actualizaciones constantes y nuevas funcionalidades basadas en feedback real.
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section
        id="cta"
        className={`relative py-32 px-4 transition-all duration-1000 delay-500 ${
          isVisible.cta ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'
        }`}
      >
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-4xl md:text-6xl font-light text-white mb-6">
            Listo para{' '}
            <span className="font-semibold bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">
              Transformar
            </span>{' '}
            tu Negocio
          </h2>
          <p className="text-xl text-zinc-400 font-light mb-12 max-w-2xl mx-auto">
            Únete a los profesionales que ya están optimizando su gestión de proyectos con Pool Calculator.
          </p>
          {isAuthenticated ? (
            <Link
              to="/dashboard"
              className="inline-block px-10 py-5 rounded-xl bg-gradient-to-r from-cyan-500 to-blue-600 text-white text-lg font-light hover:shadow-2xl hover:shadow-cyan-500/50 transition-all duration-300 hover:scale-105"
            >
              Ir al Dashboard
            </Link>
          ) : (
            <Link
              to="/register"
              className="inline-block px-10 py-5 rounded-xl bg-gradient-to-r from-cyan-500 to-blue-600 text-white text-lg font-light hover:shadow-2xl hover:shadow-cyan-500/50 transition-all duration-300 hover:scale-105"
            >
              Crear Cuenta Gratis
            </Link>
          )}
        </div>
      </section>

      {/* Footer */}
      <footer className="relative border-t border-zinc-800/50 py-12 px-4">
        <div className="max-w-6xl mx-auto text-center">
          <div className="flex items-center justify-center space-x-3 mb-6">
            <div className="w-10 h-10 rounded-xl bg-gradient-to-br from-cyan-500 to-blue-600 flex items-center justify-center shadow-lg">
              <div className="w-6 h-6 rounded-full border-2 border-white"></div>
            </div>
            <span className="text-xl font-light text-white tracking-wide">
              Pool <span className="font-semibold">Calculator</span>
            </span>
          </div>
          <p className="text-zinc-500 font-light text-sm">
            Desarrollado por profesionales para profesionales
          </p>
          <p className="text-zinc-600 font-light text-xs mt-4">
            2025 Pool Calculator. Gestión profesional de proyectos de piscinas.
          </p>
        </div>
      </footer>
    </div>
  );
};
